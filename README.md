# IC Misalignment Detection App

Ứng dụng kiểm tra lệch chân IC từ ảnh AOI.

## Cài đặt
```bash
pip install opencv-python numpy
```

## Cách sử dụng
1. Chạy `python src/tools/setup_roi.py` để lấy tọa độ 4 IC.
2. Chạy `python main.py --input data/input` để kiểm tra.
